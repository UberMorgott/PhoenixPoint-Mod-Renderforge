# Handoff — 1.4.1 RELEASED, Workshop item hidden (2026-09-06, end of session)

Repo `E:\DEV\PhoenixPoint\Renderforge`, branch `main`, HEAD `6b1a629`, tag `v1.4.1` = `fcfb35a`. Everything pushed.
Supersedes `2026-09-06-handoff-1.4.0.md` (1.4.0 was never released publicly; everything shipped as 1.4.1).

## Shipped

- GitHub release `v1.4.1`: https://github.com/UberMorgott/PhoenixPoint-Mod-Renderforge/releases/tag/v1.4.1 —
  `Renderforge-Full-1.4.1.zip` (171,419,373 B, sha256 `3499BB7B…C3C`) + `SHA256SUMS.txt`. Notes `docs\release-notes-1.4.1.md`.
- Steam Workshop item **3796708495** (https://steamcommunity.com/sharedfiles/filedetails/?id=3796708495), created
  `--create --visibility hidden`, Dist 25 files / 243.7 MB uploaded, 8 localized descriptions pushed (`--localize-descriptions`),
  preview `workshop\image\steam_preview.jpg`. **STILL HIDDEN — owner reviews the page, then**
  `python workshop\steamugc\publish_ugc.py --update --item 3796708495 --visibility public --changenote "1.4.1"`.
  Tags: none (no PP tag fits a graphics mod). Runbook `docs\OPERATIONS.md` §Workshop, `workshop\WORKSHOP.md`.
- Steam install `D:\Steam\steamapps\common\Phoenix Point\Mods\Renderforge` = 1.4.1 (backup of 1.4.0 in the session scratchpad only).

## Done this session (all Codex-reviewed `cx -Review`, opus reviewed where noted)

- Release review fixes: versions, `QualityKnobs.Disable()` in `OnModDisabled` (`1c18e1e`), re-apply knobs on re-enable (`6977d2b`),
  honest "runs instead" label (`79dc0bd`), plan checkboxes, meta description.
- Perf: `docs\research\2026-09-06-quality-knobs-perf.md` — frame time is DWM-capped at 240 Hz (borderless; exclusive fullscreen is
  converted by Win11), so cost = GPU-utilization delta at the cap: Aniso 16x +7.7 pp, all max +7.9 pp on 87 % baseline.
- Localization: `DlssConfig.Loc(en, ru)` now backed by embedded `src\Localization\strings.csv` (110 keys × 7 languages, key = EN
  string, `Renderforge.strings.csv` resource, RFC-4180 parser `DlssConfig.cs` ~:210, language = `I2.Loc.LocalizationManager.CurrentLanguageCode`).
  Regenerate skeleton after adding `Loc` calls: `scratchpad\gen-strings.ps1` was session-local — rewrite from `DlssConfig.cs` if needed
  (keys = every `Loc(` first argument + `Ru` dict). Tooltips state restrictions (`717b1de`); knob rows show `Vanilla (<live>)` (`772374e`),
  resynced after a preset apply (`1b2de91`). In-game: all 8 languages PASS programmatically (labels, descriptions, overlay, CJK glyphs);
  NOT verified visually: Options > Graphics knob rows and hover tips (PPCLI cannot open that panel — needs a human click).
- License → CC BY-NC 4.0 (`919a43a`); bilingual issue template `.github\ISSUE_TEMPLATE\` (`1681673`); repo Discussions enabled.
- Workshop tooling copied from PerkOracle (`d223314`): `workshop\{steamugc\publish_ugc.py, pack-dist.ps1, update.ps1, renderforge.vdf,
  WORKSHOP.md, image\make-previews.ps1, locale\description.<lang>.txt ×8}`; `steamugc` binaries + `Dist\` gitignored.
  Cover art generated by Codex (`cx -Img`): `image\steam_preview_src.png`, `image\github_social_src.png`; `make-previews.ps1` renders
  `steam_preview.jpg` (1024², <1 MB) and `github_social.png` (1280×640, title overlay).

## Left

1. Owner: eyeball the hidden Workshop page (8 languages, preview) → make it public (command above). Optional gallery screenshots = web UI only.
2. Owner: GitHub Settings → Social preview → upload `workshop\image\github_social.png` (no API; Chrome extension was not connected).
3. Owner: visual check of Options > Graphics knob rows / tooltips in the Steam install (1.4.1 installed).
4. Known gaps carried over: post-only never smoke-tested on AMD/Intel/GTX; 8.8 exposure identity not re-measured.

## Environment

- Instance3: game closed, `ppcli-enabled` deleted, 1.4.1 zip files installed, language `en`, knobs Vanilla, Mode Off.
- `E:\DEV\PhoenixPoint\PPCLI\ISSUES.md` unchanged by this session's agents (0 new defects; one gap noted here: Options panel not
  openable via `onClick.Invoke()` — not logged).
- Codex threads: none persisted this session (all ephemeral).
