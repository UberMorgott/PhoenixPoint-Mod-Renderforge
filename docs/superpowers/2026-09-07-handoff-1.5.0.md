# Handoff — 1.5.0 built and smoke-tested, NOT yet published (2026-09-07, mid-session context reset)

Repo `E:\DEV\PhoenixPoint\Renderforge`, `main`, HEAD `bb69e5e` (+ this doc). Local commits since the pushed `d4d1b8b` are NOT pushed.
Plan `docs\superpowers\plans\2026-09-07-1.5.0-user-feedback.md`. Release notes `docs\release-notes-1.5.0.md` (final).

## What 1.5.0 contains (all Codex-reviewed `cx -Review`, opus where risky, in-game verified on Instance2/3)

- Geoscape for LUT / colour vision (`4354738`); Levels/Contrast/Clarity native post-pass stage + 4 sliders (`12d8b3d`
  `7d2537c` `8316682` `c094988`); coalesced config saves; `SetGrade -1` keeps; tooltips as input thresholds (`47cdc8d`).
- Crisp fonts REMOVED (`939f77b`, legacy key ignored with one log line) and the unreleased Crisp icons REMOVED (`8a1a58d`):
  both measured zero pixel effect at 1440p — see `docs\research\2026-09-07-ui-sharpness-measurements.md` (`3f14ca5`).
  HUD isolation of the post pass PROVEN (outRT dump has no HUD; B&W LUT leaves HUD coloured) — an earlier "HUD graded" reading
  was semi-transparent panels over a changed scene.
- quality-gate wired (`8e22674`, `a739b45` qgate.json custom `native` check = build-native.ps1 + probes, `_typos.toml`
  `5a18580`); xunit `tests\Renderforge.Tests` 26 tests (`a9beb48`); pre-commit = `qgate -All -Full -Quiet` (~30 s).
- Version 1.5.0 (`d40ac6a`); Workshop descriptions for 1.5.0 in 8 languages (`98d1a91`, `bb69e5e`).

## Artefacts

- `build\release\Renderforge-Full-1.5.0.zip` 171,418,135 B, sha256 `183C78466A531532CE56DEA87E820C7998893DD49746F2101453E868A23770DC`
  (built at HEAD `3f14ca5`, DLL 1.5.0.0 after `8a1a58d`, no Crisp* types) + `SHA256SUMS.txt`.
- A short in-game smoke of THIS zip on Instance3 was running when the session was reset (agent `build-smoke-150b`); its result
  is not recorded — treat the zip as smoke-UNVERIFIED and re-run the short smoke (10 min) before tagging. The earlier full smoke
  (a–f PASS) was on the build one commit before (with Crisp icons still in); nothing else changed.
- Instance3 has the 1.5.0 files installed; `ppcli-enabled` should be deleted (verify), game closed.

## Remaining steps (in order)

1. `git status` — a static-analysis triage agent may have left `docs\research\2026-09-07-static-analysis-triage.md`
   uncommitted; commit it if present (`docs(research): static-analysis triage`). Findings so far: all cppcheck/clang-tidy
   "scary" ones and 16/17 Roslyn are false positives; 1 LATENT (`Dlss_Passthrough` return code dropped, `DlssDriver.cs:363,433`);
   `D3D12_HEAP_TYPE` zero-init x15 still to be traced. Post-release work.
2. Short smoke of the zip on Instance3 (scout, Opus 4.6): install zip contents, `connect state`, log shows 1.5.0 + DLSS + no
   `crisp` lines + 0 exceptions, tactical `SetGrade(40,215,150,100)` changes scene, geoscape LUT B&W greys globe / HUD coloured.
3. Tag + publish: `git tag -a v1.5.0 -m "Renderforge 1.5.0" <HEAD>`; `git push origin main v1.5.0`;
   `gh release create v1.5.0 build\release\Renderforge-Full-1.5.0.zip build\release\SHA256SUMS.txt --title "Renderforge 1.5.0" --notes-file docs\release-notes-1.5.0.md`.
4. Workshop (Steam client running, logged in): `workshop\pack-dist.ps1` (needs the fresh stage from release.ps1),
   `python workshop\steamugc\publish_ugc.py --update --item 3796708495 --changenote "1.5.0 - geoscape LUT/colour vision, Levels/Contrast/Clarity, crisp fonts removed"`,
   then `--localize-descriptions --item 3796708495`. Item is PUBLIC.
5. Steam install: game must be closed; back up `D:\Steam\steamapps\common\Phoenix Point\Mods\Renderforge` to the scratchpad,
   replace with the zip's `Renderforge\` folder (same one-liner as 1.4.1).
6. Update this handoff + memory `next-session-entry`; `graphify update E:\DEV\PhoenixPoint\Renderforge`; push.

## Post-release backlog

- Native static analysis: `.clang-format` + one reformat commit; `D3D12_HEAP_TYPE` trace; `Dlss_Passthrough` return log.
- Harmony `PatchAll` validation test (needs net472 host + game `Managed` on the probe path).
- User feedback still open: "icons" (no lever at ≤1440p — documented), ReShade parity now covered by Levels/Contrast/Clarity + NIS.

## Environment / tooling facts

- quality-gate: install `E:\DEV\quality-gate-install` (`4403e25`, on user PATH first); `QGATE_HOME=E:\Tools\qgate-home`;
  NOTHING may live on C: (owner rule). Maintainer = CCD session "AUTO TESTS" (`local_9ca67b7d-b3cf-4870-9700-566c676ef6df`,
  cwd `E:\DEV\quality-gate`) — message it with the exact `[FAIL]` line for gate bugs; never edit the gate, never bypass the hook.
  Known: fast lane vs full lane agreed after `db8cee3`; 17 Roslyn warnings on Renderforge are `[WARN]` only.
- Codex: `cx -Review E:\DEV\PhoenixPoint\Renderforge -Base <sha>` per commit set; decisions → `cx "<question>"` (owner rule
  "по решениям советуйся с кодексом"). Translations: Codex writes, opus reviews.
- Test installs: Instance2 (profile 592) and Instance3 (profile 593) both have Renderforge activated; 1440p borderless must be
  forced in Options.jopt for any visual/perf check (agents defaulted to 1262x712 twice).
- PPCLI cannot open Options > Graphics (needs a real click) — knob-row visuals are owner-checked in the Steam install.
