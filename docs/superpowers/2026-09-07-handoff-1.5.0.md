# Handoff — 1.5.0 PUBLISHED (2026-09-07)

Repo `E:\DEV\PhoenixPoint\Renderforge`, `main`, tag `v1.5.0` = `7f975f9` (pushed). Plan
`docs\superpowers\plans\2026-09-07-1.5.0-user-feedback.md`; release notes `docs\release-notes-1.5.0.md`.

## What 1.5.0 contains (all Codex-reviewed `cx -Review`, opus where risky, in-game verified on Instance2/3)

- Geoscape for LUT / colour vision (`4354738`); Levels/Contrast/Clarity native post-pass stage + 4 sliders (`12d8b3d`
  `7d2537c` `8316682` `c094988`); coalesced config saves; `SetGrade -1` keeps; tooltips as input thresholds (`47cdc8d`).
- Crisp fonts REMOVED (`939f77b`, legacy key ignored with one log line) and the unreleased Crisp icons REMOVED (`8a1a58d`):
  both measured zero pixel effect at 1440p — `docs\research\2026-09-07-ui-sharpness-measurements.md` (`3f14ca5`).
  HUD isolation of the post pass PROVEN (outRT dump has no HUD; B&W LUT leaves HUD coloured).
- quality-gate wired (`8e22674`, `a739b45`, `5a18580`); xunit `tests\Renderforge.Tests` 26 tests (`a9beb48`);
  pre-commit = `qgate -All -Full -Quiet` (~30 s).
- Version 1.5.0 (`d40ac6a`); Workshop descriptions for 1.5.0 in 8 languages (`98d1a91`, `bb69e5e`).

## Release record (2026-09-07)

- Zip `build\release\Renderforge-Full-1.5.0.zip` 171,418,135 B, sha256
  `183C78466A531532CE56DEA87E820C7998893DD49746F2101453E868A23770DC`; stage `build\release\stage\Full` byte-identical
  (DLL/native/meta hashes compared) → Workshop `Dist` built from the same bytes.
- Smoke of THIS zip on Instance3 (scout, 2560x1440 FullScreenWindow, vsync off): PASS a–d — log 1.5.0.0 / `provider=DLSS` /
  0 `crisp` / 0 exceptions; tactical `SetGrade(40,215,150,100)` mean RGB (18.4,20.3,22.0)→(14.7,16.5,17.7); geoscape B&W LUT
  globe saturated 25%→1.1% while HUD MaxSat=255; `Screen` 2560x1440. `ppcli-enabled` deleted, no PPCLI defects.
- GitHub: https://github.com/UberMorgott/PhoenixPoint-Mod-Renderforge/releases/tag/v1.5.0 — assets `Renderforge-Full-1.5.0.zip`
  + `SHA256SUMS.txt`; asset downloaded back and sha verified before Workshop.
- Workshop 3796708495: `--update` committed (`SubmitItemUpdate` OK, changenote
  "1.5.0 - geoscape LUT/colour vision, Levels/Contrast/Clarity, crisp fonts removed"), `--localize-descriptions` 8/8 OK,
  tags none, visibility unchanged (public).
- Steam install `D:\Steam\steamapps\common\Phoenix Point\Mods\Renderforge` replaced whole (not overlaid) from the
  GitHub-downloaded zip; only difference vs the old folder = 4 `nvngx*.log` files (no user config lived there).
  Backup of the old folder was in the session scratchpad (temporary).
- Rollback: `build\release\Renderforge-Full-1.4.1.zip` + GitHub `v1.4.1` release; Workshop rollback = re-upload that zip's stage.

## Post-release backlog

- Static analysis (AUTO TESTS gate sweep 2026-09-07): CA1001 on `DlssDriver.cs:21` = FALSE POSITIVE (CommandBuffers
  `Release()`d in `TeardownResources()` :459; gate will suppress CA1001 under `QGateUnity=true`). CA2101 `Native.cs:270`
  `Dlss_ProviderVersion` = real but cosmetic → add `CharSet = CharSet.Ansi` to the DllImport. CA1806 x9 worth a skim.
  Earlier triage: `Dlss_Passthrough` return code dropped (`DlssDriver.cs:363,433`, latent); `D3D12_HEAP_TYPE` zero-init x15
  still to be traced; `.clang-format` + one reformat commit.
- Harmony `PatchAll` validation test (needs net472 host + game `Managed` on the probe path).
- User feedback still open: "icons" (no lever at ≤1440p — documented); ReShade parity covered by Levels/Contrast/Clarity + NIS.
- Gate advice from AUTO TESTS (NOT applied yet): `-All -Full` should be a periodic/pre-release sweep and pre-commit should
  move to the fast lane; today `lefthook.yml` still runs `qgate -All -Full -Quiet` on every commit (~30 s). Decide + edit
  `lefthook.yml` post-release.

## Environment / tooling facts

- quality-gate: install `E:\DEV\quality-gate-install` (on user PATH first); `QGATE_HOME=E:\Tools\qgate-home`;
  NOTHING may live on C: (owner rule). Maintainer = CCD session "AUTO TESTS" (`local_9ca67b7d-b3cf-4870-9700-566c676ef6df`,
  cwd `E:\DEV\quality-gate`) — message it with the exact `[FAIL]` line for gate bugs; never edit the gate, never bypass the hook.
- Codex: `cx -Review E:\DEV\PhoenixPoint\Renderforge -Base <sha>` per commit set; decisions → `cx "<question>"`; long prompts
  via `-PromptFile`, >120 s answers via `-Deep` (background, poll the status file). Translations: Codex writes, opus reviews.
- Test installs: Instance2 (profile 592) and Instance3 (profile 593) both have Renderforge activated; 1440p borderless must be
  forced in Options.jopt for any visual/perf check (agents defaulted to 1262x712 twice).
- PPCLI cannot open Options > Graphics (needs a real click) — knob-row visuals are owner-checked in the Steam install.
- Workshop publish prerequisites and steps: `docs\OPERATIONS.md` §Workshop (Steam client logged in as Morgott, `init_test.py`,
  `pack-dist.ps1` free, `--update`/`--localize-descriptions` owner-gated).
