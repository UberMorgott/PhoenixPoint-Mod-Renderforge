# Handoff — 1.6.0 PUBLISHED (2026-09-07 evening)

Repo `E:\DEV\PhoenixPoint\Renderforge`, `main`. Tags: `v1.5.0` = `7f975f9`, `v1.5.1` = `e6873c9`, `v1.6.0` = `ec77f91`
(all pushed). Previous handoff (1.5.0/1.5.1 details, feedback list): `2026-09-07-handoff-1.5.0.md`.
Release notes `docs\release-notes-1.6.0.md`.

## 1.6.0 contents (all Codex-reviewed per commit set, opus where risky, in-game verified)

- **Grade sliders** Exposure / Brightness / Saturation / Vibrance in the native post pass (`native\Grade.h` chain
  Exposure → Levels → Brightness → Contrast → Clarity → Vibrance → Saturation, after the LUT stage; `Dlss_SetGrade` 8 params),
  "Default: N" in every slider tooltip. `33cda99` + fixes `1d0c6a1`.
- **Reset image settings** row (`src\GradePanel.cs` BuildReset: TextureQualityPicker clone, arrows hidden, label left,
  button "Reset"): resets 18 fields from `new DlssConfig()` (sharpness, LUT + strength, 8 grade knobs, scene style +
  strength + pixel size, vignette / shadow res / aniso / LOD) — keeps Renderer, Upscaler, Mode, FrameGen, ColorVision.
  `2797c10`, `e6d27a4`.
- **Resolution list dedup** (`3b56987`): transpiler on `UIModuleVideoOptionsPanel.InitResolutionPicker` → one entry per
  W×H, highest Hz (log `210 entries -> 24 unique sizes` only when the panel opens).
- **Geoscape markers without DLSS ghosting** (`e36e3a2` + rounds `b5255e3`, `a690f65`, `db3ccba`, `1c76067`):
  markers = MeshRenderer quads + TextMesh under `GeoSiteVisualsController.VisualsContainer` (NOT the "PopUp" canvases),
  no motion vectors. `src\GeoMarkerOverlay.cs` moves them to free layer 15 (ownership decided in `Camera.onPreCull` of
  GeoscapeCamera, far side by sphere test against `GlobeUnits.GlobeCenter`), strips bit 15 from every camera each frame
  (reused buffer), the driver's own `DlssPresent` camera draws the layer after the blit (blit moved to BeforeForwardOpaque
  + depth clear, only while the overlay is active). Gates: D3D11, FrameGen off, ColorVision None, curtain lifted; any
  failure disables for the level only, explicit `SetMarkerOverlay(false)` persists. Known: markers skip the game's PPv2
  (whites 255 vs 246). A separate marker camera was a measured dead end (blit never reached the backbuffer).
  DLSS bias-current-colour mask is IGNORED by preset K / 310.9 D3D11 (`33874f9` diag, results in
  `docs\research\geoscape-dlss-2026-09-07\bias-mask\`). Globe rotation for tests: `focus_pos` console + `RotationDamping=1.0`.
- **Release script**: `-WithFrameGen` removed, FG runtimes always packed (`f31b14f`).
- Pixel-perfect UI (1.5.1) unchanged. Crisp fonts stay DEAD (measured = vanilla pixels).

## Release record

- Zip `Renderforge-Full-1.6.0.zip` 171,437,854 B sha `96402B2886A070818C3C4B06E19BB0A457E8014F629CC9CA55F0C45BE9DC94A7`
  (candidate 3; candidates 1/2 smoked fully, 3 = perf-only diff + final check). stage/Dist byte-identical.
- Smokes: Instance3 full (a–e), Instance2 markers round 3 (a–f), Instance3 final candidate (a–c) — all PASS, 0 exceptions.
  Owner eyeballed sliders/reset/markers in the Steam install: ok.
- GitHub release v1.6.0 (asset downloaded back, sha verified); Workshop 3796708495 `--update` committed + 8/8 locales;
  Steam install replaced whole from the GitHub asset.
- Mist "dark rectangles" report (TFTV Oneiric Delirium screenshot, old version): NOT reproduced on 1.5.1 at 1440p
  (`docs\research\mist-rectangles-2026-09-07\results.md`); mist = `VoxelBlob` MeshRenderer + `_PX_FX/MistDecoShader`,
  no MV. Ask the reporter for version/mode on 1.6.0 before chasing.

## Rules learned today (memory has them too)

- NO piecemeal releases: finish the whole open list, then one release (owner objected to 1.5.1 following 1.5.0).
- Ship the exact smoked zip; builds are non-deterministic (DLL bytes differ per build).
- `cx -Review` needs `-Deep` and often `-Commit <sha>`; 900 s timeouts happen on big diffs.

## Backlog

- Static analysis leftovers (CA2101 `Native.cs:270` CharSet.Ansi; `Dlss_Passthrough` return; `D3D12_HEAP_TYPE`; `.clang-format`).
- Marker overlay on D3D12 / with FG (gated off now); markers through the game's post-processing (look parity).
- Harmony `PatchAll` validation test; `lefthook.yml` fast lane decision.
- Optional: attach `Renderforge-Core-<ver>.zip` (≈230 KB) to GitHub releases as an "update only" asset (owner: users
  re-download 171 MB each release) — not decided.
