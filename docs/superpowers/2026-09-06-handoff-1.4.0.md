# Handoff — 1.4.0 almost done (2026-09-06, end of session)

Repo `E:\DEV\PhoenixPoint\Renderforge`, branch `main`, HEAD `c3d3db5`, tree clean, PUSHED to
`UberMorgott/PhoenixPoint-Mod-Renderforge` at end of session. Last GitHub release: v1.3.0.
Supersedes `2026-09-05-handoff-1.4.0.md` (read that one only for the quality-knobs / colour-vision history).

## Done this session — post-only pipeline COMPLETE (plan `docs\superpowers\plans\2026-09-05-post-only-pipeline.md`, Tasks 1-9 ✅)

Every commit reviewed by Codex `cx -Review -Commit <sha>` (model gpt-6-astra) + opus spec/quality review.

| Task | Commit | Notes |
|---|---|---|
| 4 probe `--fake=N` | `8389c7c` | Codex clean (probe exit 0 for --fake=2/3/4 + D3D11/D3D12) |
| 5 P/Invoke `DLSS_OK_POST_ONLY`, `PostOnlyReason()` | `9b1c9a7` | clean |
| 6 `InitNative`/`ReinitNative` accept post-only, `Upscalers.PostCarrier` | `a6a11dd` | + fixes below |
| 7 driver: `Apply` forces Off under PostOnly, sharpness starts a generation, zero only for `DebugView.Passthrough`, re-`Apply` after async switch | `eac30a0` | |
| fix: carrier when `Running==Off` (Off pin / Auto on non-NVIDIA D3D11 → carrier = DLSS, mirrors `ProviderOf` default), NGX-probe keeps real FSR/XeSS failure, FrameGen gate `\|\| PostOnly`, `PostCarrier` reset in `OnModDisabled`, log codes | `138eb0a` | Codex P2 + opus P2/P3 |
| fix: vanilla SMAA kept in passthrough (`KeepCameraState`/`AfterApplyPostProcessOptions` gated on `passthrough`), sharpness slider enabled when `Available` | `1499859` | Codex P2 ×2 |
| fix: NGX probe returning plain `DLSS_OK` kept (reachable: `Resolve(Auto)` returns stale `Running` after disable/re-enable) | `0ee19d9` | Codex P2; `\bDLSS_OK\b` gate is now FIVE lines, all in `InitNative` |
| 8 in-game acceptance | `73d58e2` | `docs\research\2026-09-05-post-only-acceptance.md` incl. `## Re-test after fixes` |
| 9 docs | `c3d3db5` | `DESIGN.md` `### Init state machine (post-only)` + `### Dev switch RENDERFORGE_FAKE_INIT`; README; release notes |

Acceptance verdict (Instance3, RTX 5070 Ti): 8.1/8.2/8.5b/8.6/8.7/8.7b/8.7c PASS, 8.5 init codes 2/3/4 map
(never 8), 8.6 real failure → `postOnlyReason=3`. Two "defects" were harness artefacts, both proven live:
- D3D12 `gen=Idle target=null` = camera disabled behind a full-screen UI + TFTV modal (`DlssDriver.cs:221`
  `cam.isActiveAndEnabled`); in tactical D3D12 post-only is `gen=Live passthrough=True`, sharp R=2.893, LUT R=16.267.
- "NIS touches HUD" = `cmp.py` HUD crop was ~75 % scene; on an opaque UI rect Δ=0.000 for every effect.
- Known gaps (recorded in acceptance doc): 8.8 exposure identity 22.627 NOT re-measured (no `ReadExposure`
  seam), 8.9 picker screenshot skipped (Options not navigable via PPCLI), 8.7 renderer/mod-manager toggles
  untested, 8.5b third row (`first != DLSS` NGX probe) unreachable on NVIDIA. CV deut/prot scene Δ <1.0 on a dark
  map (scene-dependent, same as colour-vision plan). Non-NVIDIA/GTX never smoke-tested — docs say so.

## Left for next session (in order)

1. **Quality-knobs Task 7 (perf numbers)** — still not measured. Agent was launched then stopped at end of
   session before the game started (nothing changed on Instance3). Brief exists in this session's transcript;
   method: PresentMon 2.5.1 saw 0 presents from Unity D3D12 on 2026-09-05 → fallback = frame time via PPCLI
   `call op:get` on `Time.unscaledDeltaTime` averaged ≥300 frames + `nvidia-smi` VRAM, per preset, same tactical
   scene, `Timing.Paused`. Write `docs\research\2026-09-06-quality-knobs-perf.md`, then fix README "cost not measured".
2. **Release 1.4.0**: final whole-branch review (`cx -Review E:\DEV\PhoenixPoint\Renderforge -Base v1.3.0`, plus
   opus), smoke on Instance3 with the RELEASE zip binaries, tag `v1.4.0`, push tag, GitHub release with the single
   `Renderforge-Full-1.4.0.zip` + SHA256SUMS per `build\release.ps1` (memory `release-single-zip`).
3. Optional: `docs\release-notes-1.4.0.md` — add the perf numbers once measured.

## Environment notes

- Instance3 (`D:\PP-Instance3`) = Renderforge test install; bridge `ppcli-enabled` DELETED (resting state) —
  re-create before tests. Renderforge deployed there at `1499859` (0ee19d9 differs only in the NGX-probe branch;
  redeploy before the release smoke). Upscaler=Auto, Renderer=Auto, Mode=Off, all effect knobs 0 (left by the RCA agent).
- No game process left running. `RENDERFORGE_FAKE_INIT` not set anywhere persistent.
- `E:\DEV\PhoenixPoint\PPCLI\ISSUES.md` has UNCOMMITTED entries (PPCLI repo, not ours) from 2026-09-05 agents;
  this session's agents logged 0 new PPCLI issues.
- Renderforge code graph `graphify-out\` refreshed via `graphify update` at end of session (AST-only).
- Codex design thread for post-only: `01a07235-0aee-7631-baff-b9fa194164c7` (`cx -Resume <id>`), model gpt-6-astra.
