# Handoff — 1.4.0 in progress (2026-09-05, end of session)

Repo `E:\DEV\PhoenixPoint\Renderforge`, branch `main`, HEAD `8389c7c`, tree clean, **NOT pushed since
`d73c42c`** (everything from `dcb6f71`… onward is local only). Last release on GitHub: v1.3.0.

## Done this session (all reviewed: opus spec review + Codex `cx -Review` per commit)

- **Quality knobs** (plan `docs\superpowers\plans\2026-09-05-quality-knobs.md`, Tasks 1–6, 8 ✅):
  `src\QualityKnobs.cs`, `src\QualityPanel.cs`, rows Vignette / Shadow resolution / Anisotropic /
  LOD detail in Options → Graphics; baseline snapshot + restore before every preset switch; live
  acceptance on Instance3 PASS (all presets, invalid preset, level survival, vignette crop).
  Commits `fc3e203`…`4435a27`, docs `61a38b4`.
  **Task 7 (perf numbers) BLOCKED**: PresentMon 2.5.1 saw 0 presents from Unity D3D12; GPU was
  98 % busy (TheMound + Instance2). README says "cost not measured". Rerun when GPU idle;
  if PresentMon still sees nothing, fall back to overlay frame time + nvidia-smi VRAM.
- **Colour vision** (plan `2026-09-05-colour-vision.md`, Tasks 1–7 ✅): `native\ColorVision.h`,
  HLSL stage after Grade/style in `Sharpen.cpp`, `PostShaderEnabled` at all predicates,
  `Dlss_SetColorVision`, `ColorVisionMode` config, `src\ColorVisionPanel.cs` row between LUT and
  Scene style, `RenderforgeMod.SetColorVision`. Probes green. Live: trit PASS everywhere, deut/prot
  PASS on a bright map, FSR/XeSS-FG PASS, HUD untouched. Commits `8721ce1`…`d2f606e`, docs `0afd022`
  (incl. `docs\release-notes-1.4.0.md`).
- **Fix** `a57bef4`: D3D12-only upscaler (FSR/XeSS) pinned in config on D3D11 used to kill the
  whole post pass (LUT/style/CV) — `Upscalers.Resolve` now maps them to Auto on D3D11.
- **Research notes**: `docs\research\2026-09-05-crisp-ui-measurement.md` (icons are minified, no
  Crisp-UI feature warranted), `2026-09-05-overlay-mod-compat.md` (ContentTool findings),
  `2026-09-05-timescale-zero-harness-artifact.md` (the "freeze" was a vanilla log flood at
  timeScale=0 — pause via `Timing.Paused` in tests, never `Time.timeScale=0`).
- **Reflex standalone**: deferred to backlog (DESIGN.md) with conditions.

## In progress — post-only pipeline (post pass without an upscaler)

Plan `docs\superpowers\plans\2026-09-05-post-only-pipeline.md` (READY per Codex, 9 tasks).
Why: on non-NVIDIA/GTX D3D11 (default renderer) NGX init failure released the D3D device →
LUT/style/CV/NIS/mip bias never ran. Native side done:

| Task | Commit | Codex review |
|---|---|---|
| 1 keep device alive on NGX failure | `8c6bd57` | clean |
| 2 `DLSS_OK_POST_ONLY=8`, `Dlss_PostOnlyReason()` | `1806acb` | P2 = "managed not updated yet" → closed by Tasks 5–7 |
| 3 `RENDERFORGE_FAKE_INIT` dev switch (never calls NGX) | `65877e9` | clean |
| 4 `dlss_probe --fake=N` post-only gate (sinusoid pattern, exit 3 = untested) | `8389c7c` | **not yet reviewed** |

**Next: Tasks 5 → 9** (P/Invoke surface; `InitNative`/`ReinitNative` with `Upscalers.PostCarrier`
and Auto fallback preserved, `Upscalers.Failed` not cleared; driver runs the post pass with
upscaler Off + sharpness-only activation + debug-view-only zeroing; acceptance on Instance3 incl.
`RENDERFORGE_FAKE_INIT=2`, rename of `nvngx_dlss.dll`, restart-based Auto fallback, two consecutive
failed switches, D3D12 exposure identity 22.627; docs with NVIDIA-only qualification).
Loop per task: implementer `model:"fable"` → spec review `scout`/opus → `cx -Review -Commit <sha>`.
Codex design thread id `01a07235-0aee-7631-baff-b9fa194164c7` (`cx -Resume <id>`).

## Then

1. Task 7 of quality knobs (perf) when the GPU is idle.
2. Final whole-branch review (`cx -Review E:\DEV\PhoenixPoint\Renderforge -Base v1.3.0` or opus),
   smoke on Instance3 with the release zip binaries, tag `v1.4.0`, push, GitHub release
   (single `Renderforge-Full-1.4.0.zip` + SHA256SUMS per `build\release.ps1`).

## Environment notes

- Instance3 = Renderforge test install (ContentTool session owns Instance2). PPBridge deployed
  there (build 3068ae67) but the bridge is **disarmed** — re-create `D:\PP-Instance3\Mods\PPBridge\ppcli-enabled`.
  PPCLI client at f5878b7: `connect screenshot '{"path":...}' -Window` = final presented frame.
- `PPCLI\ISSUES.md` has new uncommitted entries from this session's agents (PPCLI repo, not ours).
- Deploy the mod with `deploy.ps1` targeting Instance3 (game closed).
