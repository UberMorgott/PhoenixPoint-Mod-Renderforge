# Retired DLSS5 / experimental Neural Rendering investigation

- Decision, 2026-09-05: the user rejected the face result and requested complete removal of experimental NR from Renderforge, including its menu, private runtime and generated experiment artifacts. Ordinary DLSS SR, DLAA, frame generation, LUTs, scene styles and font correction remain separate features.
- **No experiment achieved natural human-quality soldiers.** A successful NR evaluate call, a running FG counter, a changed face or a safe restoration is not evidence of that visual goal.
- The user rejected per-face generated bitmap packs and a large generation toolchain. The final face direction was compact code/numeric parameters preserving broad head shape. That approach also remained unproven and was stopped. Computer shutdown was explicitly cancelled.
- This dossier preserves the findings before raw diagnostic captures/caches are cleaned. Referenced detailed Markdown and compact structured evidence are historical records; their large image/probe paths may subsequently be removed. Removal/build/live acceptance is recorded separately; this document does not claim that those operations have completed.

## What actually ran

- Historical source baseline: `3252a5f6d9a9fd08cb481ae487bc1b565fac8b83`, especially `native/DlssNr12.cpp`, `native/NvngxBridge.cpp`, `src/NeuralRenderingSupport.cs` and `src/NeuralRenderingPanel.cs`.
- Private runtime `nvngx_dlssnr.dll`, version `310.8.0`, 165,840,496 bytes; SHA256 `E16BCF15E16E13F527491CDF7845B2FE6521A738D8F7C9C721866A8496E1FC8E`. The local reference hash was rechecked during retirement inventory. Gate also required a valid NVIDIA Authenticode signature, NVIDIA RTX 50-series device, D3D12 and driver at least `616.64`.
- NGX feature was `static_cast<NVSDK_NGX_Feature>(0x12)` (decimal **18**), application ID `0x0876232C`. Existing public NGX headers/types supplied ABI plumbing. This was an **experimental, unofficial private runtime integration**, not evidence of a supported public DLSS5 or RTX Neural Faces SDK.
- Private `RenderforgeNR` directory contained the NVIDIA runtime and our `nvngx.dll` bridge. Bridge wrappers called runtime exports `NVSDK_NGX_D3D12_Init_Ext`, `PopulateParameters_Impl`, `CreateFeature`, `EvaluateFeature`, `ReleaseFeature`, `Shutdown1` with guarded failures; NVIDIA binaries were not patched.
- NR processed render-resolution color/depth/motion before one normal DLSS SR/DLAA evaluation. SR parameters were reset between the calls. Runtime/create/evaluate failures were intended to fail open to ordinary DLSS.
- Bound fields included Color/Output/Depth/MVec, dimensions/subrects, reset, jitter, motion scale, reversed depth, Style, Intensity, LocalToneStrength, LocalStructureStrength, SkinStructureStrength and scalar UseAutoMask. `UICorrection` was restored to `0`; an erroneous Backbuffer alias was removed.
- There was **no verified consumed normal/roughness/head-mask texture, face identity embedding, prompt, editable anatomy or user-trained model field**. A diagnostic mask was never connected to NR. Inventing parameter names would not establish runtime consumption.
- Modes were Off/Auto; Style0/1/2 were ordinal switches, not verified distinct model choices. Strength ranges: intensity/tone/local structure `0..2`, skin structure `-1..1`; defaults `1/1/1/-1`, Style0, automatic mask enabled. Later user values were `.99/.89/.82/.26`.
- `init/create/eval=0x1`, `nrAlive=1` demonstrated execution only. RTX Neural Faces capability and substantial face reconstruction were never established. No external face model was integrated.

## Bootstrap, jitter and runtime stability

- GOG in-game D3D12 restart originally inherited `DOORSTOP_INITIALIZED` / `DOORSTOP_DISABLE`. A child could start with D3D12 yet skip Doorstop/PPModEnabler `InitMods`; the graphics flag alone was insufficient proof of mod loading.
- Commit `c672a4c` cleared those inherited variables for the restart child, retained the `RENDERFORGE_RENDERER_RESTART=1` loop guard and waited for parent exit. Related ordinary restart work preserved/quoted arguments and removed conflicting graphics flags. This lesson and the ordinary renderer switch survive NR retirement. The historical session established the GOG mod-loaded path; no new GOG launch was performed for this dossier.
- NR plus DLSS projection jitter produced a visibly shaking static globe. Commit `eb4e224` disabled projection jitter for that combination; NR Off/other providers retained their ordinary path. This traded away subpixel jitter sampling. A correct private-runtime jitter contract was never established.
- An NGX verification HUD was disabled historically with `HKLM NGXCore ShowDlssIndicator=0`, and the user confirmed the labels disappeared. This is a historical global setting, not an instruction to alter the registry during artifact cleanup.
- Moving the four NR strength sliders caused renderer/FG child-swapchain recreation because they were included in `SettingsKey`. The visible symptom was a disappearing/flickering game window, not a demonstrated desktop crash. Commit `c0e8ce2346f08249c4cc49b3f39c1ec104828988` kept strength changes on live configuration; only mode/style/automatic-mask remained structural. Configuration was snapshotted under a mutex without holding it over NGX calls.
- In 96-sample slider runs, baseline FG Off had 52 non-live/present-off observations and 44 NR-not-alive; baseline FG X2 had 49 and 33, plus 64 creates/65 shutdowns. Patched runs in each mode had all 96 live/alive observations, zero errors and zero child recreations. The user confirmed the sliders stopped flickering.
- Those background runs generated zero interpolated frames. Separate foreground evidence established FG X2: 14 samples/13 seconds, 729 rendered frames, NR alive, FG live, lastError0; native `presented=2,status=0x0`, generated count 2348 at frame7200 to 7448 at frame12300. This proves foreground FG operation separately, not simultaneous foreground slider-drag generation. See [slider lifecycle report](../2026-09-05-nr-slider-lifecycle.md).

## Head constraints and diagnostic masks

- Fixed Sophia used `Tutorial_Head_Female` (2960 vertices/5646 triangles); ordinary Yael used `Head_Hispanic1_F_V01` (2958/5646). Fixed squad members must retain their authored customization restrictions; ordinary soldiers permit hair/head switches. Bind to current visual roots and cancel stale generations on replacement.
- Both heads exposed 21 bones, zero blendshapes and readable bindposes/weights; original vertex reads were blocked by `isReadable=false`. CPU `BakeMesh` exposed posed vertices/topology/UVs, not a ready-to-use rest mesh.
- Separate eyes/lashes/hair/armor could be excluded from a skin renderer mask, but baked eyebrows and lips remained inside. Renderer selection is not semantic face segmentation.
- `BuiltinRenderTextureType.Depth` binding failed with built-in render texture type3 not found. CameraTarget depth at AfterForwardAlpha worked even when target depthBits reported0; reversed-Z handling mattered.
- Mask evidence: 49,032 visible clear-face pixels, 7,833 solid-occluder pixels, 21,058 cutout pixels, 13,225 skin pixels through holes; outside/missing pixels0; 110,708 unoccluded pixels unchanged. Sophia's helmet left968 visible of14,752 unoccluded skin pixels. Hair Next invalidated the old generation; Previous restored the original HairTag.
- Frozen DLAA comparisons used Off/default `1/1/1/-1`/maximum `2/2/2/1` and32 warm frames. NR ran but changed tone/shine, pale brows and muddy eyes rather than generating a human face. FP16 output readback converted to PNG was not an exact desktop capture; Off/Auto jitter differences also limited pixel interpretation. See [mask proof](../2026-09-05-character-mask-proof.md) and [character inventory](../2026-09-05-character-mask-inventory.md).

## Rejected face experiments

| Experiment | Actual evidence | Limit / decision |
| --- | --- | --- |
| Generated albedo | Applied experimental UV textures to both heads; one targeted Yael lip revision; gloss calibration reduced shine. | Eye/lip alignment and waxy anatomy remained. No candidate accepted; per-face bitmap packaging rejected. |
| Procedural micro-normal | Deterministic UV-stable slope amplitude `.025`, protected eyes/brows/nose/lips/ears/neck/borders; 84,328 Sophia / 5,943 Yael pixels changed. | Grainier or unchanged wax; no second strength sweep, no human-quality gain. |
| Numeric upper lids | Shared local +Y offset `.025 * interEyeBoneDistance`; Sophia `.00113918`, Yael `.00111762993`. | More startled wax; rejected. No animation-safe per-frame production hook. |
| Full rest-mesh reconstruction | Strict live preflight found Float16 UVs, then incompatible legacy/full weight values. | No clone or native independent A/B gate reached. Editable full-surface equivalence and human-quality correction remain unproved. |

- Original materials were not generic Unity Standard: Sophia `_PX_CHR/CHR_Character_shader_OLD`, Yael `_PX_CHR/CHR_Character_Corrupted_shader`; albedo2048/512 DXT1 sRGB, normal BC5 RG, metallic/gloss BC7 sRGB. Channel semantics must come from the installed shader. Original `_GlossMapScale` was **Sophia1, Yael.5**; an earlier albedo report incorrectly described both as1. Later live rereads corrected that; restoration must use observed values.
- Albedo projection/baked texture work did not alter underlying mesh identity, head shape or material architecture sufficiently. See [albedo report](../2026-09-05-face-regeneration-prototype.md).
- Micro-normal control decoded BC5 RG into RGBA32 and rebuilt mips; it was not bit-identical BC5 reconstruction. Source-to-control RGB MAE on0..255 at yaw0/45/90 was Sophia `1.865/1.158/.572`, Yael `1.061/.574/.278`; one unchanged Yael repeat measured `.932`. These conversion/control differences prohibit attributing every pixel change to the procedural effect.
- Micro-normal restoration:14 captures restored materials/released temporaries; four motion frames per head were narrow evidence only. Paired unchanged bone locals do not prove animation never writes them. Isolated diagnostic DLL8192 bytes; production package increment0. One RGBA32 mip chain is21.33MiB at2048 or1.33MiB at512, before CPU copies; observed generation325ms/21ms was not a frame-rate benchmark. See [micro-normal report](../2026-09-05-procedural-head-prototype.md).
- Lid offsets were grounded in shared parent axes and actual eyelid weights; eyesockets were separate, lash/shadow followed lids, hair did not. The initial3-degree read-only rotation prediction had undesirable depth movement and was never applied. The translation moved329/341 vertices by over1e-6; maximum displacement `.00112569809/.00121532578`.
- Lid evidence covered16 frozen views including front/45/profile NR Off plus front NR On,32 warm frames,33 observations without drift. All16 restorations reported maximum delta0 across original21 bone transform components; explicit cancel also0. A head swap destroyed old bones and cancelled the probe; that was not falsely counted as restoration of destroyed objects. See [numeric anatomy report](../2026-09-05-numeric-face-anatomy-prototype.md).

## Rest-mesh preflight: where the proof stopped

- Installed Unity2019.4 exposed `BakeMesh(Mesh)`, not a `useScale` overload. Posed normal/tangent magnitudes around1.17–1.20 must not be normalized away. A simple inverse-and-forward calculation would be tautological, not independent Unity equivalence.
- Prepared isolated algorithm planned five known weighted basis samples to calibrate position/normal/tangent skinning operators, double-precision inversion/conditioning gates, unchanged static attributes, and a second native original-versus-clone pose. Watchdog/CAS restoration protected shared mesh, bones and materials.
- First live DLL rejected `Unsupported attribute storage: TexCoord0/Float16`. Actual Sophia UV0 was Float16×2, stream1. One bounded correction used native `Object.Instantiate<T>` on the readable bake and avoided UV rewrites; offline signed-zero/exact-UV and numerical rejection tests passed, build0 warnings/0 errors. Live storage preservation was never reached.
- Retry refused **`Legacy/full weight values disagree.`** at per-bone summed-weight tolerance1e-7. The offending vertex/value pair was not exported; no measured mismatch magnitude can honestly be supplied. Quantization/renormalization is a hypothesis, not a diagnosis. The read-only sample established2960 legacy vertices and initial full-influence counts `[1,1,1,1]` only.
- Both attempts aborted before hidden calibration renderer, reconstructed clone or presentation. No independent Unity A/B geometry/normal/tangent proof or NR-Off image gate ran; Yael was not visited after the generic failure. The tolerance was not relaxed.
- Both reports recorded `originalSharedMeshRestored=true`, `originalBoneIdentityPreserved=true`, `originalMaterialsPreserved=true`, probe owners null. Original mesh1562772 and selected actor reference remained identical, config JSON matched exactly, timeScale remained1. This is exact identity/cleanup evidence; a restored geometry delta was unmeasured because no clone presentation happened. The user's manual `22222.zsav` campaign was never reloaded or written by this probe.
- Detailed report and hashes: [rest-mesh preflight](2026-09-05-rest-mesh-roundtrip-preflight.md), commit `a5bc40acfa9cd96e2a7f80075797118badd613fd`. Raw restoration reports were `D:\RenderforgeWork\head-rest-reconstruction\sophia-live\report.json` and `sophia-half-uv-live\report.json`; their verified outcomes are preserved above before cleanup.

## Optional toolchain: prepared, never used for generation

- StableProjectorz2.4.5 Minimal archive59,869,751 bytes, SHA256 `759313396A0D5F1074782E8AD9FFD937239D8DBDA067944C7453DDEDBED08741`; executable signature valid. The downloaded2025 release predated the2026 AgentBridge documentation. Actual binaries lacked SPZAgent markers; positive-control Screenshot_MGR was present. Assuming current automation APIs existed in that release would have been wrong.
- No application launch, generation backend, PyTorch/CUDA/diffusion/ControlNet install, face-model download or multiview output occurred. The discussed25GiB was a possible development workspace, not a promised mod payload. The user rejected that approach before generation.
- E: extraction contained33 files/216,453,660 bytes; dedicated D: copy31 files/156,582,349 bytes with copied-file hashes matching. Process-local cache paths were prepared; global environment was not changed, and Unity LocalLow redirection was unproved. No new dependency/toolchain downloads are authorized by this retired plan. See [historical toolchain record](../face-multiview-toolchain.md).

## Artifact retirement boundaries

- Inventory: [cleanup inventory](2026-09-05-nr-artifact-cleanup-inventory.md); exact local manifest `D:\RenderforgeWork\nr-retirement-inventory\cleanup-manifest.json`. This documentation task performed **zero deletions**.
- `docs/shots` is generated diagnostic evidence:1719 files,8,144,837,773 bytes, consisting of1716 PNG and3 Python analysis scripts. The original request already identified1719 diagnostic files; script references point to NR off/default/max comparison frames. No user source assets were found. The user's later explicit generated-artifact cleanup request superseded the earlier no-deletion-without-permission instruction.
- Preserve this dossier, compact reports/evidence and final requested font comparison; retain ordinary LUT/style validation. Source/build/install runtime removal belongs to the respective code/deployment owners. Mixed rollback backups must not be deleted wholesale before deployment validation.
- Durable lessons: respect authored fixed/ordinary head identity; inspect shader/mesh contracts; measure controls before visual claims; distinguish package bytes from CPU/GPU working memory; preserve safe cleanup; reject unproven private-runtime assumptions. None of those lessons establishes a successful face enhancer.

## Completed distinct screenshot/temp cleanup

- After dossier commit `624ae639bc09b4e0bb56df19541e39959e0a040f`, the authorized separate cleanup removed2290 files /8,565,395,555 bytes: five explicitly resolved roots and45 selected docs files. Those roots were `docs/shots`, `D:\RenderforgeTools\face-multiview`, and `D:\RenderforgeWork\procedural-head`, `numeric-anatomy`, `head-rest-reconstruction`.
- Every recursive target was checked against the exact allowlist, descendant containment and absence of reparse points. Exact per-target outcome: `D:\RenderforgeWork\nr-retirement-inventory\cleanup-receipt.json`, status `complete`; no policy rejection occurred for this distinct batch.
- Final requested font comparison SHA256 was identical before/after. Compact primary JSON and reports, ordinary LUT/style proof, active NR-removal validation, game state, build/**, refs/** and installed-game paths were untouched. The earlier native build-cleanup rejection was not retried.
- Removed tracked diagnostic source and images remain recoverable from Git history before the cleanup commit. Historical links now lead to preserved findings rather than missing media. Package/runtime removal and deployment remain separately owned operations.
